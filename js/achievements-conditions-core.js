/** Conditions des achievements solo (hors histoire). */
export const ACHIEVEMENTS_CONDITIONS = {
    premiere_ligne: (stats) => stats.lignesTotal >= 1,
    premier_tetris: (stats) => stats.maxLignesUnCoup >= 4,
    centenaire: (stats) => stats.lignesTotal >= 100,
    millenaire: (stats) => stats.lignesTotal >= 1000,
    score_10k: (stats) => stats.meilleurScore >= 10000,
    score_50k: (stats) => stats.meilleurScore >= 50000,
    score_100k: (stats) => stats.meilleurScore >= 100000,
    survivant_3min: (stats) => stats.meilleurTemps >= 180,
    survivant_10min: (stats) => stats.meilleurTemps >= 600,
    explorateur: (stats) => stats.biomesJoues.size >= 3,
    globe_trotter: (stats) => stats.biomesJoues.size >= 9,
    inferno_survivor: (stats) => (stats.meilleurTempsParBiome.lave || 0) >= 300,
    cosmos_master: (stats) => (stats.lignesParBiome.cosmos || 0) >= 50,
    reliquaire: (stats) => stats.reliquesUtilisees >= 10,
    collectionneur: (stats) => stats.typesReliquesUtilises.size >= 9,
    meteorologue: (stats) => stats.meteosSubies >= 5,
    chaos_maitrise: (stats) =>
        (stats.meteosVues || new Set()).has('blizzard') &&
        (stats.meteosVues || new Set()).has('inversion'),
    combo_fou: (stats) => stats.maxCombo >= 5,
    robo_ami: (stats) => stats.reactionsRobo >= 20,
    compositeur: (stats) => stats.maxNotesComposition >= 15,
    grand_maitre: (stats) => stats.nbAchievementsDebloques >= 10,
    oracle_debutant: (s) => (s.oracleDeviationsPartieActuelle || 0) >= 5,
    oracle_maitre: (s) => (s.oracleMeilleuresMult || 1) >= 4.0,
    premiers_pas_coop: (s) => (s.lignesCoopTotal || 0) >= 10,
    synchro_parfaite: (s) => (s.coopMaxLignesUnCoup || 0) >= 4,
    archi_premier: (s) => (s.archiNiveauxCompletes?.size || 0) >= 1,
    archi_etoiles: (s) => (s.archiEtoilesMax || 0) >= 15,
    archi_parfait: (s) => (s.archiPrecisionMax || 0) >= 100,
    archi_econome: (s) => (s.archiParAtteint || 0) >= 1,
    vivant_premier: (s) => (s.evenementsVivantSubis || 0) >= 1,
    vivant_survivant: (s) => (s.maxEvenementsUnePartie || 0) >= 10,
    vivant_tous_biomes: (s) => (s.biomesVivantSubis?.size || 0) >= 7,
    vivant_maitre: (s) => (s.lignesPendantVivant || 0) >= 50,
};
